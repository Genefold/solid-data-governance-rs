//! CLI crate — binary entry points are in serve.rs and test_runner.rs.
//!
//! solid-server  →  src/serve.rs       (see [[bin]] in Cargo.toml)
//! solid-test    →  src/test_runner.rs  (see [[bin]] in Cargo.toml)
//!
//! This file intentionally contains no code; it exists only so that editors
//! and documentation tools that expect a crate root do not emit warnings.
