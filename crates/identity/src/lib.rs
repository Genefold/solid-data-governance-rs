//! Identity subsystem: accounts, pods, WebIDs, OIDC, client credentials.
//!
//! Mirrors the TypeScript `src/identity` module hierarchy.

pub mod account;
pub mod pod;
pub mod webid;
pub mod client_credentials;
